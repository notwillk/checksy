{
  "id": "rulesy",
  "version": "1.0.1",
  "name": "Rulesy",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "Release version to install, with or without a leading v; latest and current select the latest release"
    }
  }
}

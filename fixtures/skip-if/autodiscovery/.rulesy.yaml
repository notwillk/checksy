rules:
  - name: entrypoint predicate
    skip-if: |
      : "${RULESY_SKIP_IF_ENTRYPOINT:?entrypoint mode is required}"
      : > ".${RULESY_SKIP_IF_ENTRYPOINT}.predicate"
      exit 0
    check: |
      : > ".unexpected-${RULESY_SKIP_IF_ENTRYPOINT}-check"
      exit 89

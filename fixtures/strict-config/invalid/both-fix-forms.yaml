rules:
  - name: Must not execute before later validation failure
    check: 'test -n "$RULESY_TEST_MARKER" && printf ran > "$RULESY_TEST_MARKER"'
  - check: 'false'
    fix: touch ready
    interactive-fix: read -r answer

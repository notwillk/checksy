rules:
  - name: failed rule needs an interactive repair
    severity: error
    timeout: 2s
    check: "false"
    interactive-fix: ': > "$RULESY_UNEXPECTED_INTERACTIVE"'
  - name: reporting continues after unavailable repair
    severity: error
    timeout: 2s
    check: ': > "$RULESY_LATER_RULE_MARKER"'

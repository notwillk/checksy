rules:
  - name: TERM-resistant managed process tree
    severity: error
    timeout: 2h
    check: |
      trap '' INT TERM HUP QUIT
      export RULESY_HELPER_ROLE=leader
      exec "$RULESY_PROCESS_HELPER" --ignored --exact process_tree_helper --nocapture

rules:
  - name: Root command must wait for cached Git validation
    check: 'test -n "$CHECKSY_TEST_MARKER" && printf root > "$CHECKSY_TEST_MARKER"'
  - remote: git+https://example.invalid/checks.git#main:remote.yaml

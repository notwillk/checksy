unexpected: true
rules:
  - name: Neither check nor fix may execute
    check: 'test -n "$RULESY_TEST_CHECK_MARKER" && printf check > "$RULESY_TEST_CHECK_MARKER"; false'
    fix: 'test -n "$RULESY_TEST_FIX_MARKER" && printf fix > "$RULESY_TEST_FIX_MARKER"'

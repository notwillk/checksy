# Git-based Remote Config Test
# References the Rulesy repo itself via git SSH URL
# Uses the happy-path fixture from the Rulesy repository

cachePath: ".git-cache"
rules:
  - remote: git+git@github.com:notwillk/rulesy.git#main:fixtures/happy-path/.rulesy.yaml
  - name: Local verification
    check: echo "Git remote test completed"
    severity: info

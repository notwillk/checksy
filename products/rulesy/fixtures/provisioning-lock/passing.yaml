rules:
  - name: passing check still provisions under the semaphore
    check: |
      printf 'pass\n' >> "$RULESY_PROVISION_TRACE"
      true
    fix: |
      printf 'unexpected-fix\n' >> "$RULESY_PROVISION_TRACE"
      exit 91
